[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  command = "node scripts/start-vite-loopback.mjs"
  targetPort = 8000
  port = 8888
  publish = "dist"
